My PhD thesis written with Classic Thesis Style. To see the polish version of the thesis checkout "polish" branch.

To compile the thesis use LyX with https://github.com/jzftran/pythontex-wrapper.py and Tex Live distribution.

Because the thesis uses Python packages, before compilation it may be necessary to install them by running:

`pip install -r requirements.txt`